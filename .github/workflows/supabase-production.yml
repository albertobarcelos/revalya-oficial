name: Deploy Supabase - Production

on:
  push:
    branches:
      - main
    paths:
      - 'supabase/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy automático para Produção
    environment: production  # Requer aprovação manual se configurado

    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_PROJECT_ID: ${{ secrets.PRODUCTION_PROJECT_ID }}

    steps:
      - name: Checkout código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Necessário para detectar mudanças

      - name: Configurar Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Autenticar no Supabase
        run: |
          supabase login --token "$SUPABASE_ACCESS_TOKEN"

      - name: Linkar ao projeto Production
        run: supabase link --project-ref $SUPABASE_PROJECT_ID

      - name: Detectar mudanças nas migrations (apenas mudanças de develop)
        id: detect-migrations
        run: |
          # Se for merge de develop para main, detectar apenas mudanças desde develop
          if [ "${{ github.event_name }}" == "push" ]; then
            # Tentar encontrar o commit anterior (antes do merge)
            PREV_COMMIT="${{ github.event.before }}"
            if [ -z "$PREV_COMMIT" ] || [ "$PREV_COMMIT" == "0000000000000000000000000000000000000000" ]; then
              # Se não conseguir, verificar se há mudanças em migrations
              if git diff --name-only HEAD~1 HEAD | grep -q '^supabase/migrations/'; then
                echo "has_migrations=true" >> $GITHUB_OUTPUT
                echo "✅ Novas migrations detectadas"
              else
                echo "has_migrations=false" >> $GITHUB_OUTPUT
                echo "ℹ️ Nenhuma migration nova"
              fi
            else
              if git diff --name-only $PREV_COMMIT ${{ github.sha }} | grep -q '^supabase/migrations/'; then
                echo "has_migrations=true" >> $GITHUB_OUTPUT
                echo "✅ Novas migrations detectadas"
              else
                echo "has_migrations=false" >> $GITHUB_OUTPUT
                echo "ℹ️ Nenhuma migration nova"
              fi
            fi
          else
            # workflow_dispatch - aplicar todas as migrations pendentes
            echo "has_migrations=true" >> $GITHUB_OUTPUT
            echo "✅ Deploy manual - aplicando todas as migrations"
          fi

      - name: Detectar mudanças nas Edge Functions (apenas mudanças de develop)
        id: detect-functions
        run: |
          # Se for merge de develop para main, detectar apenas mudanças desde develop
          if [ "${{ github.event_name }}" == "push" ]; then
            PREV_COMMIT="${{ github.event.before }}"
            if [ -z "$PREV_COMMIT" ] || [ "$PREV_COMMIT" == "0000000000000000000000000000000000000000" ]; then
              if git diff --name-only HEAD~1 HEAD | grep -q '^supabase/functions/'; then
                echo "has_functions=true" >> $GITHUB_OUTPUT
                echo "✅ Mudanças nas Edge Functions detectadas"
              else
                echo "has_functions=false" >> $GITHUB_OUTPUT
                echo "ℹ️ Nenhuma mudança nas Edge Functions"
              fi
            else
              if git diff --name-only $PREV_COMMIT ${{ github.sha }} | grep -q '^supabase/functions/'; then
                echo "has_functions=true" >> $GITHUB_OUTPUT
                echo "✅ Mudanças nas Edge Functions detectadas"
              else
                echo "has_functions=false" >> $GITHUB_OUTPUT
                echo "ℹ️ Nenhuma mudança nas Edge Functions"
              fi
            fi
          else
            # workflow_dispatch - deployar todas as functions
            echo "has_functions=true" >> $GITHUB_OUTPUT
            echo "✅ Deploy manual - deployando todas as Edge Functions"
          fi

      - name: Aplicar migrações (apenas mudanças de develop)
        if: steps.detect-migrations.outputs.has_migrations == 'true'
        run: |
          echo "🚀 Aplicando migrations na produção..."
          echo "⚠️  ATENÇÃO: Isso irá modificar o banco de dados de produção!"
          supabase db push --project-ref $SUPABASE_PROJECT_ID

      - name: Deploy Edge Functions (apenas mudanças de develop)
        if: steps.detect-functions.outputs.has_functions == 'true'
        run: |
          echo "🚀 Fazendo deploy das Edge Functions na produção..."
          supabase functions deploy --project-ref $SUPABASE_PROJECT_ID

      - name: Deploy todas as Edge Functions (se migrations foram aplicadas)
        if: steps.detect-functions.outputs.has_functions == 'false' && steps.detect-migrations.outputs.has_migrations == 'true'
        run: |
          echo "🚀 Fazendo deploy de todas as Edge Functions (após migrations)..."
          supabase functions deploy --project-ref $SUPABASE_PROJECT_ID

      - name: Resumo do deploy
        run: |
          echo "## 🚀 Deploy Produção Concluído" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Projeto:** Produção (main)" >> $GITHUB_STEP_SUMMARY
          echo "**Branch Git:** main" >> $GITHUB_STEP_SUMMARY
          echo "**Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Autor:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ steps.detect-migrations.outputs.has_migrations }}" == "true" ]; then
            echo "✅ Migrations aplicadas" >> $GITHUB_STEP_SUMMARY
          else
            echo "ℹ️ Nenhuma migration nova para aplicar" >> $GITHUB_STEP_SUMMARY
          fi
          if [ "${{ steps.detect-functions.outputs.has_functions }}" == "true" ]; then
            echo "✅ Edge Functions deployadas" >> $GITHUB_STEP_SUMMARY
          else
            echo "ℹ️ Nenhuma mudança nas Edge Functions" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Dashboard:** https://supabase.com/dashboard/project/${{ secrets.PRODUCTION_PROJECT_ID }}" >> $GITHUB_STEP_SUMMARY
