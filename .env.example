# =====================================================
# CONFIGURAÇÕES DO SUPABASE
# =====================================================

# URL do projeto Supabase
# Obtenha em: https://app.supabase.com/project/[seu-projeto]/settings/api
VITE_SUPABASE_URL=https://seu-projeto.supabase.co

# Chave pública anônima do Supabase
# Obtenha em: https://app.supabase.com/project/[seu-projeto]/settings/api
VITE_SUPABASE_ANON_KEY=sua-chave-anonima-aqui

# Chave de service role (apenas para operações administrativas)
# ATENÇÃO: Esta chave deve ser mantida em segredo e nunca exposta no frontend
# Use apenas em Edge Functions ou operações server-side
VITE_SUPABASE_SERVICE_ROLE_KEY=sua-chave-service-role-aqui

# =====================================================
# CONFIGURAÇÕES DE DESENVOLVIMENTO
# =====================================================

# Modo de desenvolvimento
VITE_DEV_MODE=true

# URL base da aplicação
VITE_APP_URL=http://localhost:8080

# =====================================================
# CONFIGURAÇÕES DE INTEGRAÇÃO
# =====================================================

# WhatsApp Business API
VITE_WHATSAPP_API_URL=
VITE_WHATSAPP_API_TOKEN=

# Gateway de pagamento
VITE_PAYMENT_GATEWAY_URL=
VITE_PAYMENT_GATEWAY_KEY=

# =====================================================
# CONFIGURAÇÕES DE AUTENTICAÇÃO MULTI-TENANT
# =====================================================

# Chave privada JWT para assinatura de tokens (gerada automaticamente)
# IMPORTANTE: Esta chave deve ser única por ambiente e mantida em segredo
JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----
# Cole aqui sua chave privada RSA/ECDSA gerada
# Gere com: openssl genpkey -algorithm RSA -out private_key.pem -pkcs8 -pass pass:your_password
-----END PRIVATE KEY-----

# URL base do portal para redirecionamentos
PORTAL_BASE_URL=https://portal.revalya.com

# Configurações de expiração de tokens
REFRESH_TOKEN_EXPIRY_DAYS=30
ACCESS_TOKEN_EXPIRY_HOURS=1

# Configurações de segurança
MAX_REFRESH_TOKENS_PER_USER=10
ENABLE_TOKEN_ROTATION=true
ENABLE_SUSPICIOUS_ACTIVITY_DETECTION=true

# =====================================================
# CONFIGURAÇÕES DE MONITORAMENTO
# =====================================================

# Sentry (opcional)
VITE_SENTRY_DSN=

# Analytics (opcional)
VITE_ANALYTICS_ID=

# =====================================================
# NOTAS IMPORTANTES
# =====================================================

# 1. Copie este arquivo para .env e preencha com seus valores reais
# 2. Nunca commite o arquivo .env no repositório
# 3. Variáveis que começam com VITE_ são expostas no frontend
# 4. A chave SERVICE_ROLE deve ser usada apenas server-side
# 5. Para produção, configure essas variáveis no seu provedor de hosting